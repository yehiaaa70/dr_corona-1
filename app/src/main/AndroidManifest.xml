<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.side">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Side">
        <activity android:name=".ui.logout.register_equipment" />
        <activity android:name=".ui.logout.register_nurse" />
        <activity android:name=".ui.logout.register_patient" />
        <activity android:name=".ui.logout.registar_dr" />
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Side.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.logout.register" />
        <activity android:name=".ui.logout.login" />
        <activity android:name=".ui.home.critical_issues" />
        <activity android:name=".ui.doctor.doctor_profile" />
        <activity android:name=".ui.scannerView" />




    </application>

</manifest>